#!/bin/bash

    yad --icons --read-dir=/usr/share/applications --geometry=224x640+244+42 \
    --sort-by-name --single-click --skip-taskbar \
    --title="Icon Menu" --no-buttons --escape-ok \
    --window-icon=applications-other \
