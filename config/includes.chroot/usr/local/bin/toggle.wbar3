#!/bin/bash
trap 'kill $(jobs -p) >/dev/null 2>&1' EXIT
# kills its children on exit ^
# your custom wbar start here


if pgrep -f wbar-conf> /dev/null; then
    pkill -f wbar-conf
else
    wbar --config ~/.config/wbar/wbar-conf
fi
