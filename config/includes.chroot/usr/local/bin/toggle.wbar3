#!/bin/bash
trap 'kill $(jobs -p) >/dev/null 2>&1' EXIT
# kills its children on exit ^
# your custom wbar start here


if pgrep -f wbar-configs> /dev/null; then
    pkill -f wbar-configs
else
    wbar --config ~/.config/wbar/wbar-configs
fi
