#!/bin/bash
# Small script to change to fluxbox during session.
pkill -f stalonetray
pkill -f plank
pkill -f compton
pkill -f ld-hotcorners
pkill -f fbpanel
pkill -f tint2
pkill -x openbox
/usr/bin/startfluxbox