#!/bin/bash
# Small script to change to fluxbox during session.
pkill -f stalonetray
pkill -f plank
pkill -f ld-hotcorners
pkill -f conky
pkill -f compton
pkill -f fbpanel
pkill -f tint2
pkill -xf pekwm & /usr/bin/startfluxbox